Run 

python3 -m http.server --cgi

from this directory to start the local server.

Then open "http://localhost:8000/index.html" in the browser.

Also, this can be accessed using the local IP address from other computers in the same netowrk.

Kui pordi suunamine on sätestatud telia iseteeninduses, näiteks
	lähteport 8000
	sihtport 8000
	IP-aadress sama mis local IP address (terminalist "ifconfig", väärtus "inet")
siis saab public IP aadressiga html lehele ligi ka mistahes arvutist.
